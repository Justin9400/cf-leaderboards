import { Stack } from "@mui/system";

function Home() {
    return(
        <Stack sx={{ml: 8, display: 'flex', height: '100vh'}}>
            <h1>HOME</h1>
        </Stack>
    );
}

export default Home;